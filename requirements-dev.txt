
pytest==8.2.2
pytest-asyncio==0.24.0
pytest-html==4.1.1
hypothesis==6.103.1
pytest-cov==5.0.0

# Версии синхронизированы с .pre-commit-config.yaml — раньше эти инструменты
# нигде не устанавливались как pip-пакеты, поэтому CI падал на "ruff: command not found".
# types-redis сюда НЕ добавляем: это устаревший пакет заглушек, конфликтующий с
# нативными inline-типами (PEP 561), которые redis==5.2.1 уже несёт сам по себе —
# с ним mypy сыпет полсотней ложных ошибок вместо нуля.
ruff==0.9.5
mypy==1.11.2
bandit[toml]==1.7.9