# Copy to .env.migrator; fill independently; never copy full root .env.
SERVICE_ROLE=migrator
LOG_PSEUDONYM_KEY=REPLACE_WITH_RANDOM_64_HEX_CHARACTERS
MIGRATION_DATABASE_URL=postgresql+asyncpg://alert_bot_migrator:REPLACE@db.example.com/alerts
